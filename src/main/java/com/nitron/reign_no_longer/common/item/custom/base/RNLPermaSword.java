package com.nitron.reign_no_longer.common.item.custom.base;

import net.minecraft.item.SwordItem;
import net.minecraft.item.ToolMaterial;

public class RNLPermaSword extends SwordItem {
    public RNLPermaSword(ToolMaterial toolMaterial, int attackDamage, float attackSpeed, Settings settings) {
        super(toolMaterial, attackDamage, attackSpeed, settings);
    }
}
